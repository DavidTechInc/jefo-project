const {express} = require('express')

const {crazydb} = require('crazypaiement')

.getNumber(__dirname),if(!Developper) await crazy.sendMessage("paiement acceptés veuillez attendre votre ticket dans 2 minutes");

{ ApiAirtel ='https://airteldevelopper/apiauth2/paiement';}

const {e} =('error detected in ${e}')
                         }
         }
     
};
